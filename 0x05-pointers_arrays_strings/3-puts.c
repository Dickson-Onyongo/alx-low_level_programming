#include <stdio.h>
/**
 * _puts - prints a string followed by new line.
 * @*str: string pointer.
 * @Mystr: the string
 * Return: returns 0 upon success.
 */

void _puts(char *str)
{
	char Mystr[];
	char *str = &Mstr;

	_puts(str);


	return (0);
}
