#include <stdio.h>

/**
 * main -erves as the entry point of the program
 *
 * return: always return 0
 */
int main(void)
{
		puts("\"Programming is like building a multilingual puzzle");
		return (0);
}
